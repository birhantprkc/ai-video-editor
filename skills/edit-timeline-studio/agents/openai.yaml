interface:
  display_name: "AI Video Editing with Timeline Studio"
  short_description: "Precise video editing, timeline markers, and subject effects"
  default_prompt: "Use $edit-timeline-studio to inspect the project, plan edits or timeline markers, preview the semantic diff, and validate a new editable .timeline project; for video edits, consider source-time speed curves, Color Wheels, and verified subject effects, then render the requested video."
